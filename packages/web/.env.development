# Loaded by Vite when running `pnpm dev` (mode=development).
# Vite merges this with .env.local — Cognito IDs siguen viniendo de ahí.
# Para usar prod desde dev temporalmente, override en .env.development.local.
VITE_API_BASE_URL=http://localhost:3000
