{
  "name": "@smart-wallet/infra-sls",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "serverless package",
    "clean": "rm -rf .serverless .turbo",
    "deploy": "serverless deploy",
    "dev": "IS_OFFLINE=true DYNAMODB_ENDPOINT=http://localhost:8000 serverless offline start",
    "package": "serverless package",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no tests yet' && exit 0",
    "init:local": "tsx scripts/init-local-table.ts"
  },
  "dependencies": {
    "@smart-wallet/api": "workspace:*"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.1045.0",
    "esbuild": "^0.25.4",
    "serverless": "^3.40.0",
    "serverless-esbuild": "^1.53.0",
    "serverless-offline": "^13.9.0",
    "tsx": "^4.21.0"
  }
}
