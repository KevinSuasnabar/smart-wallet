{
  "name": "@smart-wallet/infra-cdk",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist .turbo cdk.out *.tsbuildinfo",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "synth": "cdk synth",
    "deploy": "cdk deploy SmartWalletProdStack --require-approval never",
    "diff": "cdk diff",
    "destroy": "cdk destroy SmartWalletProdStack",
    "test": "echo 'no tests yet' && exit 0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.189.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "aws-cdk": "^2.189.0",
    "tsx": "^4.21.0"
  }
}
